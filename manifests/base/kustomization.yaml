apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


labels:
  - pairs:
      app: go-app
    includeSelectors: true
    includeTemplates: true

resources:
  - serviceaccount.yaml
  - externalsecret.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - pdb.yaml
